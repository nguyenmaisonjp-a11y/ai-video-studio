import { ResearchEngine } from '../engines/researchEngine.js'
import { OutlineEngine } from '../engines/outlineEngine.js'
import { ScriptEngine } from '../engines/scriptEngine.js'

export const engines = {
  ResearchEngine,
  OutlineEngine,
  ScriptEngine,
}
